# config/model/catboost.yaml

model:
  type: catboost

params:
  loss_function: Logloss
  depth: 6
  learning_rate: 0.05
  iterations: 500
  l2_leaf_reg: 3.0
  eval_metric: AUC
  random_seed: 42
  verbose: false
