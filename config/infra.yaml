# config/infra.yaml
project:
  name: nba_career_longevity
  environment: local # local | staging | prod

paths:
  data_dir: data
  raw_data: data/raw/nba_players.csv
  train_data: data/processed/train.csv
  test_data: data/processed/test.csv
  artifacts_dir: artifacts
  logs_dir: logs

mlflow:
  experiment_name: nba_career_longevity

runtime:
  random_state: 42
  log_level: INFO

